package cards

import texts.*
import utils.texts.*

open class BarCards
{
	val barCards : List<Card> = listOf(
			Card(nameBarJack, meaningBarJack),
			Card(nameBarQueen, meaningBarQueen),
			Card(nameBarKing, meaningBarKing),
			Card(nameBarAce, meaningBarAce),
			Card(nameBar2, meaningBar2),
			Card(nameBar3, meaningBar3),
			Card(nameBar4, meaningBar4),
			Card(nameBar5, meaningBar5),
			Card(nameBar6, meaningBar6),
			Card(nameBar7, meaningBar7),
			Card(nameBar8, meaningBar8),
			Card(nameBar9, meaningBar9),
			Card(nameBar10, meaningBar10),
	                                  )
}
